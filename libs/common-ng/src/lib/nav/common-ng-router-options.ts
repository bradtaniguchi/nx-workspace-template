import { ExtraOptions } from '@angular/router';

/**
 * Common settings that should be activated for all Angular projects.
 */
export const COMMON_NG_ROUTER_OPTIONS: ExtraOptions = {
  paramsInheritanceStrategy: 'always',
};
